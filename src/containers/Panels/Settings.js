import React, { useState } from "react";
import { makeStyles, useTheme } from '@material-ui/core';
const useStyles = makeStyles((theme) => ({

    

}));


const Settings = () => {
    const classes = useStyles();
    return (
        <React.Fragment>
            <h2 style={{color:"white"}}>SETTINGS MENU</h2>
        </React.Fragment>
    );
};

export default Settings;